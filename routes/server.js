const Todo = require('../models/todo');

class Server {
    getTodos() {}

    createTodo() {
        try {
        } catch (e) {
            throw new Error(e);
        }
    }

    editTodo() {}

    deleteTodo() {}
}

module.exports = new Server();
